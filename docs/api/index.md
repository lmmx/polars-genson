# Reference

- [API](polars_genson.md): every public function and the `df.genson` DataFrame methods,
  with their parameters, return values and examples, generated from the docstrings.
- [Options](../reference/options.md): every option in one place, grouped by what it
  affects, with its default and the functions that accept it.

For what the options do to your data, see [How it works](../concepts/index.md).
